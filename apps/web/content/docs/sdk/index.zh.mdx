---
title: SDK
description: 用于将 Security Layer 集成到 AI Agent 应用中的原生 TypeScript SDK。
icon: Package
---

## 为什么使用 SDK？

CLI 集成（hooks、shell shim）无需修改代码即可工作——但它们在边界层运行。SDK 为你提供**进程内安全保障**，并具备完整的上下文信息：

| | Shell Shim | Hooks | SDK |
|---|---|---|---|
| 拦截命令执行 | 是 | 是 | 是 |
| 拦截文件写入 | 否 | 是 | 是 |
| 拦截网络请求 | 否 | 是 | 是 |
| 完整会话上下文 | 否 | 部分 | **是** |
| Taint tracking 精度 | 粗粒度 | 中等 | **逐操作** |
| 绕过风险 | PATH 篡改 | 无 | **无（进程内）** |
| 适用任意框架 | 是（仅 shell） | 仅 Claude Code | **任意框架** |
| 集成工作量 | 零 | 一条命令 | 代码改动 |

当你正在构建 AI Agent 应用，并希望获得结构性的安全保障——而不仅仅是命令行拦截时，SDK 是正确的选择。

## 安装

<Tabs items={["npm", "yarn", "pnpm", "bun"]}>
  <Tab value="npm">
    ```bash
    npm install @securitylayerai/sdk
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn add @securitylayerai/sdk
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add @securitylayerai/sdk
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add @securitylayerai/sdk
    ```
  </Tab>
</Tabs>

## 快速示例

```typescript
import { createSecurityLayer } from "@securitylayerai/sdk";

const sl = await createSecurityLayer();

// Check an action before executing it
const result = await sl.check("exec", { command: "rm -rf /tmp/output" });

if (result.decision === "DENY") {
  console.error(`Blocked: ${result.reason}`);
} else if (result.decision === "REQUIRE_APPROVAL") {
  const approved = await sl.waitForApproval(result.approvalId as string);
  if (!approved) console.error("Approval denied");
}

// Clean up when done
sl.destroy();
```

## 集成模式

SDK 支持三种集成模式，具体取决于你的 Agent 框架的结构：

<Cards>
  <Card
    title="直接检查"
    description="在每次工具执行前调用 sl.check()。"
    href="/docs/sdk/patterns#direct-check"
    icon={<Code weight="duotone" />}
  />
  <Card
    title="中间件包装"
    description="使用 withSecurityLayer() 透明地包装工具执行器。"
    href="/docs/sdk/patterns#middleware-wrapper"
    icon={<Wrench weight="duotone" />}
  />
  <Card
    title="基于事件"
    description="接入 beforeToolUse / afterToolUse 生命周期事件。"
    href="/docs/sdk/patterns#event-based"
    icon={<Lightning weight="duotone" />}
  />
</Cards>

## 下一步

<Cards>
  <Card
    title="快速入门"
    description="设置、配置和你的第一次安全检查。"
    href="/docs/sdk/getting-started"
    icon={<Lightning weight="duotone" />}
  />
  <Card
    title="API 参考"
    description="所有 SDK 方法和类型的完整参考。"
    href="/docs/sdk/api-reference"
    icon={<Code weight="duotone" />}
  />
</Cards>
