---
title: Instalación
description: Instala el CLI y los requisitos del sistema.
icon: Rocket
---

## Requisitos del sistema

- **Runtime:** [Bun](https://bun.sh) v1.0 o posterior
- **SO:** macOS o Linux
- **Opcional:** Docker (para la base de datos del panel web)

## Instalar el CLI

<Tabs items={["npm", "yarn", "pnpm", "bun"]}>
  <Tab value="npm">
    ```bash
    npm install -g securitylayer
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn global add securitylayer
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add -g securitylayer
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add -g securitylayer
    ```
  </Tab>
</Tabs>

## Verificar la instalación

```bash
sl --version
```

También puedes usar el nombre completo del comando:

```bash
securitylayer --version
```

## Qué incluye

El paquete `securitylayer` instala:

- **`sl` / `securitylayer`** — El binario del CLI
- **`@securitylayer/core`** — El motor de seguridad (capability gate, taint tracker, normalizer, rules, LLM judge)

## Siguientes pasos

Ejecuta el asistente de configuración interactivo:

```bash
sl init
```

Consulta [Init](/es/docs/getting-started/init) para un recorrido completo, o ve directo a [Inicio rápido](/es/docs/getting-started/quick-start) para una guía completa de inicio a fin.
