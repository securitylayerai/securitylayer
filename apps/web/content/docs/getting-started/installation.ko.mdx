---
title: 설치
description: CLI 및 시스템 요구사항을 설치합니다.
icon: Rocket
---

## 시스템 요구사항

- **런타임:** [Bun](https://bun.sh) v1.0 이상
- **OS:** macOS 또는 Linux
- **선택사항:** Docker (웹 대시보드 데이터베이스용)

## CLI 설치

<Tabs items={["npm", "yarn", "pnpm", "bun"]}>
  <Tab value="npm">
    ```bash
    npm install -g securitylayer
    ```
  </Tab>
  <Tab value="yarn">
    ```bash
    yarn global add securitylayer
    ```
  </Tab>
  <Tab value="pnpm">
    ```bash
    pnpm add -g securitylayer
    ```
  </Tab>
  <Tab value="bun">
    ```bash
    bun add -g securitylayer
    ```
  </Tab>
</Tabs>

## 설치 확인

```bash
sl --version
```

전체 명령어 이름도 사용할 수 있습니다:

```bash
securitylayer --version
```

## 포함 항목

`securitylayer` 패키지에 포함되는 항목:

- **`sl` / `securitylayer`** — CLI 바이너리
- **`@securitylayer/core`** — 보안 엔진 (capability 게이트, taint 추적기, 정규화기, 규칙, LLM 심판)

## 다음 단계

대화형 설정 마법사를 실행합니다:

```bash
sl init
```

전체 안내는 [Init](/ko/docs/getting-started/init)을, 5분 만에 완료하는 가이드는 [빠른 시작](/ko/docs/getting-started/quick-start)을 참고하세요.
