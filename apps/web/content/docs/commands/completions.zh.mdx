---
title: completions
description: 输出 shell 补全脚本。
icon: Terminal
---

生成 shell 补全脚本，用于 Security Layer 命令和参数的 Tab 补全。

## 用法

```bash
sl completions <shell>
```

## 参数

| 参数 | 默认值 | 描述 |
|---|---|---|
| `<shell>` | `bash` | Shell 类型：`bash`、`zsh` 或 `fish` |

## 安装

### Bash

```bash
sl completions bash > /etc/bash_completion.d/securitylayer
```

### Zsh

```bash
sl completions zsh > "${fpath[1]}/_securitylayer"
```

### Fish

```bash
sl completions fish > ~/.config/fish/completions/securitylayer.fish
```

## 退出码

| 代码 | 含义 |
|---|---|
| `0` | 补全脚本输出成功 |
| `1` | 不支持的 shell |
