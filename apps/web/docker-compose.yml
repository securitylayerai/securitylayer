services:
  db:
    image: postgres:alpine
    ports:
      - 5432:5432
    volumes:
      - securitylayer_dev:/var/lib/postgresql/data
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=securitylayer_dev

volumes:
  securitylayer_dev:
