{
  "name": "@securitylayer/adapters",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "vitest run --passWithNoTests",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@securitylayer/core": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5"
  }
}
