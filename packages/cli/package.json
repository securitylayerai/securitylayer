{
  "name": "securitylayer",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "bin": {
    "securitylayer": "bin/securitylayer.ts",
    "sl": "bin/securitylayer.ts"
  },
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "vitest run",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@securitylayerai/core": "workspace:*",
    "@bomb.sh/args": "latest",
    "@bomb.sh/tab": "latest",
    "@clack/prompts": "latest",
    "yaml": "^2"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5"
  }
}
