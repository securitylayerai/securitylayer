{
  "name": "@securitylayerai/sdk",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun --external @securitylayerai/core",
    "test": "vitest run",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "@securitylayerai/core": "workspace:*"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5"
  }
}
