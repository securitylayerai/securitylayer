{
  "name": "@securitylayer/core",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "vitest run",
    "typecheck": "bunx tsc --noEmit"
  },
  "dependencies": {
    "pino": "^9.6.0",
    "yaml": "^2.7.1",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5"
  }
}
